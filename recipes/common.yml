---
# yaml-language-server: $schema=https://schema.blue-build.org/recipe-v1.json
modules:
  - type: dnf
    install:
      packages:
        - alacritty

        - emacs
        - libvterm
        - mupdf
        - mupdf-devel

  - type: signing
